import eventsSchema from "./events-schema";
import issuesSchema from "./issues-schema";
import newsSchema from "./news-schema";

const schemas = [newsSchema, eventsSchema, issuesSchema];

export default schemas;
